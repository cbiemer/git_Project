/**@jsx React.DOM */
var React      = require('react');
var AppActions = require('../actions/action');

var Stat = React.createClass({
	render:function(){
		return(
			<center>
				<h2>Loading</h2>
			</center>
		)
	}
});

module.exports = Stat;